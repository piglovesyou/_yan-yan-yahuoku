style
  html,body {height: 100%}
  #target {
    margin:100px;
    padding: 20px;
    width:100px;
    height: 100px;
    background: lightgreen;
    /* background:rgba(0,0,0,.05); */
  }


div#target
  | yeah

script
  var menu = app.ui.ContextMenu.getInstance();
  var component = new goog.ui.Component();
  component.decorate(goog.dom.getElement('target'));
  goog.events.listen(component.getElement(), 'mousedown', function (e) {
    menu.launch(e, [
      {content: '9', desc: '9999999999999'},
      {content: 'b', desc: 'bbbbbbbbbbbb'},
      {content: 'g', desc: 'gggggggggggggg'},
      {content: 'l', desc: 'zzzzzzzzzzzzz'},
      {content: 'j', desc: 'ooooooooooo'}
    ]);
  });

  // var dialog = new app.ui.Dialog();
  // goog.events.listen(goog.dom.getElement('target'), 'mousedown', function (e) {
  //   // TODO* position is something wrong/
  //   dialog.setVisible(true);
  // });

  // dialog.launch('/about');
  // dialog.setContent('yaahh<br />eee');
  // goog.dom.append(dialog.getTitleTextElement(),
  //   goog.dom.createDom('img', {src:'/images/logo-large.png'}));
  // dialog.setButtonSet(null);
  // dialog.setVisible(true);
